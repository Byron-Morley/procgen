package com.liquidpixel.main.services;

import com.liquidpixel.core.api.ui.IUIService;

/**
 * Simple UI service for the example
 * In your real project, replace with your actual UI service
 */
public class SimpleUIService implements IUIService {

    public boolean isMouseOverUI() {
        // In a real app, check if mouse is over UI elements
        return false;
    }
}
