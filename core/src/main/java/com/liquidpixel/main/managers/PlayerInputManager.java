package com.liquidpixel.main.managers;

import com.liquidpixel.core.api.ui.IUIService;
import com.liquidpixel.core.core.CorePlayerInputManager;

public class PlayerInputManager extends CorePlayerInputManager {
    public PlayerInputManager(IUIService uiService) {
        super(uiService);
    }
}
